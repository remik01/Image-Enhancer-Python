"""Image Workbench package root.

This package is intentionally thin in Phase 02. It establishes the importable
project boundary so later phases can add domain, application, adapter, and
bootstrap behavior behind architecture tests instead of relying on prose alone.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
