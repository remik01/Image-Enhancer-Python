"""Domain layer for image workbench concepts and invariants.

Future domain types belong here when they express project meaning without
depending on UI, HTTP, persistence, AI, plugin, or image-processing libraries.
"""
