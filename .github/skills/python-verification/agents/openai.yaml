interface:
  display_name: "Python Verification"
  short_description: "[Python] Run and interpret pytest, ruff, type-checking, packaging, and CI-like checks."
  default_prompt: "Use $python-verification to run the focused verification for the current Python changes."
